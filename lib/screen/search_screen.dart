import 'package:flutter/material.dart';

class SearchScreen extends StatelessWidget {
  const SearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    print('SearchScreen build');
    return Center(
      child: Text('Search Screen'),
    );
  }
}
